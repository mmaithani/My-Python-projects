# os.environ['SDL_VIDEO_CENTERED'] = '1' # Center the window (optional).
		
import pygame as pg
pg.init()
pygame.display.set_mode((640,480)) # Set screen size of pygame window
pygame.Surface(screen.get_size())  # Create empty pygame surface
fill((255,255,255))     # Fill the background white color (red,green,blue)
# background = background.convert()  # Convert Surface to make blitting faster
# screen.blit(background,(0,0))
